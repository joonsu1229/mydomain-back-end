<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.domainreg.persistence.mapper.OrderMapper">

    <resultMap id="OrderMap" type="com.domainreg.core.entity.Order">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="domainId" column="domain_id"/>
        <result property="orderNumber" column="order_number"/>
        <result property="amount" column="amount"/>
        <result property="currency" column="currency"/>
        <result property="productType" column="product_type"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="OrderMap">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <select id="findByOrderNumber" resultMap="OrderMap">
        SELECT * FROM orders WHERE order_number = #{orderNumber}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (user_id, domain_id, order_number, amount, currency, product_type, status)
        VALUES (#{userId}, #{domainId}, #{orderNumber}, #{amount}, #{currency}, #{productType}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE orders SET status = #{status}, updated_at = NOW() WHERE id = #{id}
    </update>

</mapper>
