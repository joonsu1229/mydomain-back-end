<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.domainreg.persistence.mapper.UserMapper">

    <resultMap id="UserMap" type="com.domainreg.core.entity.User">
        <id property="id" column="id"/>
        <result property="loginId" column="login_id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="nsEnabled" column="ns_enabled"/>
        <result property="privacyEnabled" column="privacy_enabled"/>
        <result property="domainLimit" column="domain_limit"/>
        <result property="emailVerified" column="email_verified"/>
        <result property="verificationToken" column="verification_token"/>
        <result property="failedLoginAttempts" column="failed_login_attempts"/>
        <result property="lockedUntil" column="locked_until"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="lastLoginIp" column="last_login_ip"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="UserMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findByEmail" resultMap="UserMap">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="findByLoginId" resultMap="UserMap">
        SELECT * FROM users WHERE login_id = #{loginId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (login_id, name, email, password_hash, phone, role, email_verified, verification_token)
        VALUES (#{loginId}, #{name}, #{email}, #{passwordHash}, #{phone}, #{role}, #{emailVerified}, #{verificationToken})
    </insert>

    <update id="update">
        UPDATE users SET
            name = #{name},
            phone = #{phone},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM users WHERE email = #{email})
    </select>

    <select id="existsByLoginId" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM users WHERE login_id = #{loginId})
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users
    </select>

    <select id="findByVerificationToken" resultMap="UserMap">
        SELECT * FROM users WHERE verification_token = #{token}
    </select>

    <update id="verifyEmail">
        UPDATE users SET email_verified = TRUE, verification_token = NULL, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateVerificationToken">
        UPDATE users SET verification_token = #{token}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="findAll" resultMap="UserMap">
        SELECT * FROM users ORDER BY created_at DESC
    </select>

    <!-- Login security queries -->
    <update id="recordLoginFailure">
        UPDATE users SET
            failed_login_attempts = failed_login_attempts + 1,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="lockAccount">
        UPDATE users SET
            locked_until = #{lockedUntil},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="recordLoginSuccess">
        UPDATE users SET
            failed_login_attempts = 0,
            locked_until = NULL,
            last_login_at = NOW(),
            last_login_ip = #{ip},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="unlockAccount">
        UPDATE users SET
            failed_login_attempts = 0,
            locked_until = NULL,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE users SET
            password_hash = #{passwordHash},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateEmail">
        UPDATE users SET
            email = #{email},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateDomainLimit">
        UPDATE users SET
            domain_limit = #{domainLimit},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePermissions">
        UPDATE users SET ns_enabled = #{nsEnabled}, privacy_enabled = #{privacyEnabled}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deletePaymentsByUserId">
        DELETE FROM payments WHERE order_id IN (SELECT id FROM orders WHERE user_id = #{id})
    </delete>

    <delete id="deleteOrdersByUserId">
        DELETE FROM orders WHERE user_id = #{id}
    </delete>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
